# X AIリプ アシスタント

X（旧Twitter）の交流リプを効率化する Chrome 拡張機能です。
**Xリプ専用の Claude プロジェクト（または ChatGPT のGPTチャット）を
使い続ける**構成で、**API・バックエンドサーバー・APIキーは不要**です。

- 各投稿に「AIリプ」ボタンを表示
- 投稿の本文と投稿者名を取得してプロンプトを自動作成
  （リプ生成の固定ルールはAI側＝Claudeプロジェクト／GPTに設定。
    拡張機能からは【最優先指示】＋投稿情報＋出力形式だけを送る）
- プロンプトをクリップボードへコピーし、画面に大きく
  「コピーしました。⌘＋Vで貼り付け、Enterで送信してください」と表示
- 設定したAIチャットのタブを前面表示（無ければ設定したURLを開く）
- 使うAIサービスは設定画面で **Claude / ChatGPT** を切り替え可能
- AI が生成した案をコピーして X に戻ると、対象投稿の返信欄へ自動入力
- **X にも AI チャットにも自動貼り付け・自動送信はしません。**
  **最終送信は必ず人間が行います。**
- AI チャットの画面（入力欄・送信ボタン）は一切操作しません

## 構成

```
X-reply-ai/
├── README.md
├── extension/          … Chrome 拡張機能（これだけで動く）
│   ├── manifest.json   … 拡張機能の設計図（権限: tabs / storage / clipboardWrite）
│   ├── settings.js     … 設定の既定値と読み込み・プロンプト組み立ての共通処理
│   ├── options.html    … 設定画面（専用チャットURL・プロンプト・案内文）
│   ├── options.js      … 設定画面の保存・読み込み処理
│   ├── content.js      … ボタン追加・投稿取得・コピー・返信欄への入力
│   ├── background.js   … ChatGPTタブを探して前面表示／専用チャットを開く係
│   └── content.css     … ボタンなどの見た目
├── tests/              … 自動テスト
│   ├── e2e-noduplicate.js    … 返信欄への重複入力が起きないことの確認
│   ├── e2e-service-switch.js … Claude / ChatGPT 切り替えの確認
│   └── fixtures/             … X の返信欄（Draft.js）を再現したテストページ
└── server/             … 【休止中】OpenAI API 版のバックエンド
```

## 関連ツール

- `x-account-analyzer/` … 参考アカウントの投稿データを時系列で分析し、
  0→1（初収益）達成のプロセスを解き明かすローカルWebアプリ（Streamlit製）。
  使い方は [x-account-analyzer/README.md](x-account-analyzer/README.md) を参照。

## 自動テストの実行方法

```bash
cd tests
npm install          # 初回のみ（Playwrightのブラウザも必要: npx playwright install chromium）
npm test
```

### 使用する権限（必要最小限）

| 権限 | 用途 |
|---|---|
| `tabs` | 開いているタブから ChatGPT のタブを探し、前面に切り替える |
| `storage` | 設定（専用チャットURL・プロンプト・案内文）を保存する |
| `clipboardWrite` | プロンプトをクリップボードへコピーする |

## Chrome への読み込み方（開発版）

1. Chrome で `chrome://extensions` を開く
2. 右上の「デベロッパーモード」をオンにする
3. 「パッケージ化されていない拡張機能を読み込む」をクリック
4. このプロジェクトの `extension` フォルダを選択する
5. https://x.com を開く（すでに開いていたらページを再読み込みする）

ファイルを修正したときは、`chrome://extensions` で拡張機能の
「更新」ボタン（🔄）を押してから、X のページを再読み込みしてください。

## 初期設定（専用チャット／プロジェクトの登録）

1. 使いたいAIサービス側に、Xリプ専用の場所を用意して固定ルール
   （禁止事項・視点の優先順位・3案の形式など）を設定する
   - **Claude の場合**: プロジェクトを作り、プロジェクトの指示に設定
   - **ChatGPT の場合**: カスタムGPTを作り、GPTの指示に設定
2. そのURLをコピー
   - Claude: https://claude.ai/project/xxxx
   - ChatGPT: https://chatgpt.com/g/g-xxxx
3. `chrome://extensions` → 「X AIリプ アシスタント」の「詳細」→「拡張機能のオプション」
   （またはツールバーの拡張機能アイコンを右クリック →「オプション」）
4. 設定画面で以下を保存:
   - **使うAIサービス** … Claude か ChatGPT を選択（初期値は Claude）
   - **ClaudeプロジェクトのURL／GPTチャットのURL** … 手順2のURL
   - **リプ生成プロンプト（固定プロンプト）** … 初期値のままでOK。
     {{author}} が投稿者名、{{postText}} が投稿本文に置き換わる
   - **案内文** … 初期値のままでOK

## 使い方

1. タイムラインの投稿の「AIリプ」ボタンを押す
2. プロンプトがコピーされ、画面に大きく
   「コピーしました。⌘＋Vで貼り付け、Enterで送信してください」と表示された後、
   設定したAIチャット（Claudeプロジェクト等）のタブに切り替わる
   （タブが無ければ設定したURLが新しいタブで開く。毎回新規チャットは作らない。
     コピーに失敗した場合は赤いエラーが表示される）
3. AIチャットの入力欄に **貼り付け（⌘V）→ 送信**（あなたの操作）
4. 生成された3案（本命・親しみ・知見）から気に入ったものをコピー
5. X のタブに戻り、画面右下の **「📋 コピーした文章を返信欄へ入力」** を押す
   - 対象の投稿（AIリプボタンを押した投稿）の返信欄が開き、自動入力される
   - 初回は Chrome が「クリップボードの読み取りを許可しますか」と
     聞いてくるので「許可」を選ぶ
6. 内容を確認・編集して、**送信は自分で押す**

うまく自動入力できない場合も、文章はコピーされたままなので
返信欄に手動で貼り付ければ OK です。

## 開発ロードマップ

- [x] ステップ1: モック版（固定3案の表示）
- [x] ステップ1.5: ルールベースのリプ生成（投稿解析＋視点の優先順位）
- [x] ステップ2: バックエンド（ルールベース生成を返すモックサーバー）
- [x] ステップ3: 拡張機能とバックエンドの接続
- [x] ステップ5: 選んだ案を返信欄へ自動入力（送信は手動のまま）
- [x] ステップ4: バックエンドと OpenAI API の接続（.env でキー管理）
- [x] ステップ6: ChatGPT（Web版）連携に切り替え（API・サーバー不要に）
- [x] ステップ7: 専用チャット継続利用＋設定画面（URL・プロンプト・案内文）
- [x] ステップ8: プロンプトを短文化（固定ルールは ChatGPT プロジェクト側へ）
- [x] ステップ9: 正式仕様に整理（専用GPTチャット＋大きな案内表示＋新プロンプト形式）
- [x] ステップ10: Claude（claude.ai）対応。設定で Claude / ChatGPT を切り替え可能に

## 【休止中】OpenAI API 版について

API 版のコード一式はブランチ **`backup/openai-api-version`** に保存してあります。
現在のブランチにも `server/` フォルダを残しています
（起動: `cd server && npm install && npm start`、APIキーは `server/.env` に設定）。
API 版に戻す場合は、バックアップブランチの拡張機能コードを復元してください。
