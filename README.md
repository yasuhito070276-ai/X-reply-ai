# X AIリプ アシスタント

X（旧Twitter）の交流リプを効率化する Chrome 拡張機能です。

- 各投稿に「AIリプ」ボタンを表示
- 投稿の本文と投稿者名を取得
- リプを3案生成（本命・親しみ・知見）
  - 現在はルールベース版：投稿を解析して「読者が次に知りたいこと」
    （具体例・初心者向けの入り口・注意点・判断基準・例外ケース）を補足する
- 選んだ文章を返信に使える
- **送信の自動化はしません。最終送信は必ず人間が行います。**

## 構成

```
X-reply-ai/
├── README.md
├── extension/          … Chrome 拡張機能
│   ├── manifest.json      … 拡張機能の設計図
│   ├── reply-generator.js … リプ生成ロジック（ルールベース）
│   ├── content.js         … ボタン追加・投稿取得・候補パネル表示
│   └── content.css        … ボタンとパネルの見た目
└── server/             … バックエンド（今後追加予定）
```

APIキーは拡張機能内には保存しません。AI（OpenAI API）への通信は
バックエンドサーバー経由で行い、キーはサーバーの環境変数で管理します。

## Chrome への読み込み方（開発版）

1. Chrome で `chrome://extensions` を開く
2. 右上の「デベロッパーモード」をオンにする
3. 「パッケージ化されていない拡張機能を読み込む」をクリック
4. このプロジェクトの `extension` フォルダを選択する
5. https://x.com を開く（すでに開いていたらページを再読み込みする）

## 動作確認

1. タイムラインの各投稿の下（返信・リポストのボタン列の右）に
   「AIリプ」ボタンが表示される
2. ボタンを押すと、投稿の本文・投稿者名・採用した視点と、
   リプ3案（本命・親しみ・知見）がパネルに表示される
3. 候補をクリックするとクリップボードにコピーされる
   （返信欄への自動入力は今後のステップで実装）

`content.js` を修正したときは、`chrome://extensions` で拡張機能の
「更新」ボタン（🔄）を押してから、X のページを再読み込みしてください。

## 開発ロードマップ

- [x] ステップ1: モック版（固定3案の表示）
- [x] ステップ1.5: ルールベースのリプ生成（投稿解析＋視点の優先順位）
- [ ] ステップ2: バックエンド（モック応答を返すサーバー）
- [ ] ステップ3: 拡張機能とバックエンドの接続
- [ ] ステップ4: バックエンドと OpenAI API の接続
- [ ] ステップ5: 選んだ案を返信欄へ入力（送信は手動のまま）
