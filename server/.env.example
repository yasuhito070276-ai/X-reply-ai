# このファイルは .env の「見本」です。
# 同じフォルダに .env という名前でコピーして、実際の値を書き込んでください。
#   cp .env.example .env
# .env は .gitignore に登録済みなので、GitHub に上がることはありません。

# OpenAI の APIキー（https://platform.openai.com で取得）
OPENAI_API_KEY=

# 使用するモデル（OpenAI側の名称変更にコード修正なしで追従するため変更可能）
OPENAI_MODEL=gpt-5.5
